{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // ── Conexión directa a PostgreSQL local (sin SqlBridge) ──────────────────────
  // DbService detecta esta clave y conecta directo, ignorando SqlBridgeUrl.
  // Reemplazá los valores con los de tu Postgres local.
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5433;Database=comercio;Username=postgres;Password=michael;"
  }
}
